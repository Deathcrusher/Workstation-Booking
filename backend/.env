PORT=3000
JWT_SECRET=your-secret-key-here
DATABASE_URL=postgresql://user:password@localhost:5432/booking_system?schema=public

# Default credentials for the initial administrator account.  In production,
# override these values via environment variables or secrets to avoid using
# predictable defaults.  If not set, the server will fall back to the
# hard-coded defaults defined in `ensureDefaultAdmin`.
# DEFAULT_ADMIN_EMAIL=admin@admin.com
# DEFAULT_ADMIN_PASSWORD=admin
